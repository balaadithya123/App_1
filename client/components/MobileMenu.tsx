import { Menu, X, UserRound, LogOut, BriefcaseBusiness, Search, Bell, Heart, Clock3, ShieldCheck } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
import { getSavedWorkerIds } from "@/lib/favorites";
import { getRecentlyViewedWorkerIds } from "@/lib/recently-viewed";
export default function MobileMenu(){const navigate=useNavigate();const[open,setOpen]=useState(false);const[session,setSession]=useState<any>(null);const[unread,setUnread]=useState(0);const[savedCount,setSavedCount]=useState(0);const[recentCount,setRecentCount]=useState(0);useEffect(()=>{if(!supabase)return;let active=true;void supabase.auth.getSession().then(({data})=>{if(active)setSession(data.session)});const{data:l}=supabase.auth.onAuthStateChange((_e,next)=>{if(active)setSession(next)});return()=>{active=false;l.subscription.unsubscribe()}},[]);useEffect(()=>{const refresh=()=>{setSavedCount(getSavedWorkerIds().length);setRecentCount(getRecentlyViewedWorkerIds().length)};refresh();window.addEventListener("saved-workers-changed",refresh);window.addEventListener("recently-viewed-changed",refresh);return()=>{window.removeEventListener("saved-workers-changed",refresh);window.removeEventListener("recently-viewed-changed",refresh)}},[]);useEffect(()=>{let active=true;setUnread(0);if(!session||session.user?.user_metadata?.role==="worker")return;void fetch("/api/notifications",{headers:{Authorization:`Bearer ${session.access_token}`},cache:"no-store"}).then(async r=>r.ok?r.json():null).then(result=>{if(active&&result)setUnread((result.notifications||[]).filter((n:any)=>!n.read_at).length)}).catch(()=>{});return()=>{active=false}},[session]);const logout=async()=>{await supabase?.auth.signOut();setOpen(false);navigate("/",{replace:true})};const loggedIn=!!session;const isWorker=session?.user?.user_metadata?.role==="worker";const showInbox=loggedIn&&!isWorker;return <div className="relative z-50 flex items-center"><button type="button" aria-label={open?"Close menu":"Open menu"} aria-expanded={open} onClick={()=>setOpen(v=>!v)} className="flex h-10 w-10 items-center justify-center rounded-[10px] border border-slate-300 bg-white text-slate-900 shadow-sm dark:border-slate-800 dark:bg-[#050505] dark:text-white">{open?<X size={20}/>:<Menu size={21}/>}</button>{open&&<nav className="absolute right-0 top-12 z-[100] w-60 rounded-[12px] border border-slate-200 bg-white p-2 shadow-lg dark:border-slate-800 dark:bg-[#080808]"><Link to="/" onClick={()=>setOpen(false)} className="flex items-center gap-2 rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><Search size={16}/>Find Workers</Link><Link to="/saved" onClick={()=>setOpen(false)} className="flex items-center justify-between rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><span className="flex items-center gap-2"><Heart size={16}/>Saved Workers</span>{savedCount>0&&<span className="text-xs text-teal">{savedCount}</span>}</Link><Link to="/recently-viewed" onClick={()=>setOpen(false)} className="flex items-center justify-between rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><span className="flex items-center gap-2"><Clock3 size={16}/>Recently Viewed</span>{recentCount>0&&<span className="text-xs text-teal">{recentCount}</span>}</Link>{isWorker&&<><Link to="/worker-dashboard" onClick={()=>setOpen(false)} className="flex items-center gap-2 rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><BriefcaseBusiness size={16}/>Worker Portal</Link><Link to="/profile-completeness" onClick={()=>setOpen(false)} className="flex items-center gap-2 rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><ShieldCheck size={16}/>Profile Completeness</Link></>}{showInbox&&<Link to="/inbox" onClick={()=>setOpen(false)} className="flex items-center justify-between rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><span className="flex items-center gap-2"><Bell size={16}/>Inbox</span>{unread>0&&<span className="text-xs text-teal">{unread>9?"9+":unread}</span>}</Link>}{loggedIn&&<><Link to="/profile" onClick={()=>setOpen(false)} className="flex items-center gap-2 rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white"><UserRound size={16}/>Profile</Link><button type="button" onClick={logout} className="flex w-full items-center gap-2 rounded-[9px] px-3 py-3 text-left text-sm font-bold text-navy dark:text-white"><LogOut size={16}/>Logout</button></>}{!loggedIn&&<Link to="/login" onClick={()=>setOpen(false)} className="block rounded-[9px] px-3 py-3 text-sm font-bold text-navy dark:text-white">Login</Link>}</nav>}</div>}
