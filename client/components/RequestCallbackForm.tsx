import { useState, type FormEvent } from "react";
import { X } from "lucide-react";
import { supabase } from "@/lib/supabase";
import { logAnalyticsEvent } from "@/lib/analytics";
type Props={workerId?:string;agencyId?:string;workerName?:string;agencyName?:string;service:string;onClose:()=>void};
export default function RequestCallbackForm({workerId,agencyId,workerName,agencyName,service,onClose}:Props){const [name,setName]=useState(""),[phone,setPhone]=useState(""),[serviceNeeded,setServiceNeeded]=useState(service),[preferredTime,setPreferredTime]=useState(""),[notes,setNotes]=useState(""),[saving,setSaving]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");const targetName=agencyName||workerName||"the provider";const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setError("");setMessage("");if(!supabase){setError("Callback requests are temporarily unavailable.");return}const normalizedPhone=phone.replace(/\D/g,"");if(!/^\d{10}$/.test(normalizedPhone)){setError("Enter a valid 10-digit phone number.");return}if(!workerId&&!agencyId){setError("No callback target was selected.");return}setSaving(true);try{const response=await fetch("/api/callback-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({workerId:workerId||null,agencyId:agencyId||null,clientName:name.trim(),clientPhone:normalizedPhone,serviceNeeded:serviceNeeded.trim(),preferredTime:preferredTime.trim(),notes:notes.trim()||null})});const data=await response.json().catch(()=>null) as {message?:string}|null;if(!response.ok)throw new Error(data?.message||"Unable to send the callback request right now.");void logAnalyticsEvent("callback_submitted",workerId||agencyId||null,{service:serviceNeeded.trim(),target_type:agencyId?"agency":"worker"});setMessage(`Request sent for ${targetName}.`);setName("");setPhone("");setServiceNeeded(service);setPreferredTime("");setNotes("")}catch(callbackError){setError(callbackError instanceof Error?callbackError.message:"Unable to send the callback request right now. Please try again")}finally{setSaving(false)}};return <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/50 p-4 sm:items-center" role="dialog" aria-modal="true"><section className="w-full max-w-md rounded-[16px] border border-line bg-white p-5 shadow-2xl dark:border-white/10 dark:bg-[#151515]"><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-extrabold text-navy dark:text-white">Request a callback</h2><p className="mt-1 text-xs text-slate dark:text-slate-300">Ask {targetName} to contact you.</p></div><button type="button" onClick={onClose} aria-label="Close" className="rounded-full p-2"><X size={18}/></button></div><form onSubmit={submit} className="mt-5 space-y-3"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" className="h-11 w-full rounded-[9px] border border-line bg-white px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/><input required inputMode="numeric" maxLength={10} value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,"").slice(0,10))} placeholder="10-digit phone number" className="h-11 w-full rounded-[9px] border border-line bg-white px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/><input required value={serviceNeeded} onChange={e=>setServiceNeeded(e.target.value)} placeholder="Service needed" className="h-11 w-full rounded-[9px] border border-line bg-white px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/><input required value={preferredTime} onChange={e=>setPreferredTime(e.target.value)} placeholder="Preferred time" className="h-11 w-full rounded-[9px] border border-line bg-white px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/><textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={3} placeholder="Notes (optional)" className="w-full resize-none rounded-[9px] border border-line bg-white px-3 py-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/>{error&&<p className="text-center text-xs font-semibold text-red-600">{error}</p>}{message&&<p className="rounded-[9px] bg-emerald-50 px-3 py-2 text-center text-xs font-semibold text-emerald-700">{message}</p>}<button type="submit" disabled={saving} className="flex h-11 w-full items-center justify-center rounded-[9px] bg-navy text-sm font-bold text-white disabled:opacity-60">{saving?"Sending...":"Request callback"}</button></form></section></div>}
