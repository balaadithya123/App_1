import { useEffect, useState } from "react";
import { CalendarDays, CheckCircle2, Clock3, XCircle } from "lucide-react";
import { useNavigate } from "react-router-dom";
import PageShell from "@/components/PageShell";
import { supabase } from "@/lib/supabase";

type Commitment={id:string;job_type:string;location:string|null;description:string|null;start_time:string;end_time:string;status:string;cancellation_reason:string|null;worker_notes:string|null};
const tabs=["upcoming","past","cancelled"] as const;
export default function WorkerCommitments(){const navigate=useNavigate();const [tab,setTab]=useState<typeof tabs[number]>("upcoming");const [items,setItems]=useState<Commitment[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{if(!supabase){setLoading(false);return;}const {data:userData}=await supabase.auth.getUser();const user=userData.user;if(!user||user.user_metadata?.role!=="worker"){navigate("/",{replace:true});return;}const {data,error}=await supabase.from("commitments").select("id,job_type,location,description,start_time,end_time,status,cancellation_reason,worker_notes").eq("worker_id",user.id).order("start_time",{ascending:true});if(!error)setItems((data??[]) as Commitment[]);setLoading(false);})();},[navigate]);const filtered=items.filter(x=>tab==="upcoming"?(x.status==="pending"||x.status==="confirmed"):(tab==="past"?x.status==="completed":(x.status==="cancelled"||x.status==="no_show")));return <PageShell backTo="/worker-dashboard" backLabel="Worker Portal"><section className="rounded-[18px] border border-line bg-white p-5 dark:border-white/10 dark:bg-[#111] sm:p-7"><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-teal">Worker Portal</p><h1 className="mt-2 text-3xl font-extrabold text-navy dark:text-white">My Commitments</h1><p className="mt-2 text-sm text-slate dark:text-slate-300">Your upcoming, completed and cancelled jobs.</p><div className="mt-6 grid grid-cols-3 gap-2">{tabs.map(t=><button key={t} onClick={()=>setTab(t)} className={`rounded-[10px] px-3 py-2.5 text-xs font-extrabold capitalize ${tab===t?"bg-navy text-white dark:bg-white dark:text-black":"border border-line text-slate dark:border-white/10 dark:text-slate-300"}`}>{t}</button>)}</div></section><section className="mt-5 space-y-3">{loading?<p className="rounded-[14px] border border-line bg-white p-6 text-center text-sm text-slate dark:border-white/10 dark:bg-[#111] dark:text-slate-300">Loading commitments...</p>:filtered.length===0?<p className="rounded-[14px] border border-line bg-white p-6 text-center text-sm text-slate dark:border-white/10 dark:bg-[#111] dark:text-slate-300">No {tab} commitments yet.</p>:filtered.map(job=><article key={job.id} className="rounded-[15px] border border-line bg-white p-4 dark:border-white/10 dark:bg-[#111]"><div className="flex items-start justify-between gap-3"><div><h2 className="font-extrabold text-navy dark:text-white">{job.job_type}</h2><p className="mt-1 text-xs text-slate dark:text-slate-300">{job.location||"Location not set"}</p></div><span className="rounded-full border border-black/10 px-2.5 py-1 text-[11px] font-bold capitalize dark:border-white/10">{job.status.replace("_"," ")}</span></div><div className="mt-3 grid gap-2 text-xs text-slate dark:text-slate-300 sm:grid-cols-2"><span className="flex items-center gap-1.5"><CalendarDays size={14}/>{new Date(job.start_time).toLocaleDateString(undefined,{day:"numeric",month:"short",year:"numeric"})}</span><span className="flex items-center gap-1.5"><Clock3 size={14}/>{new Date(job.start_time).toLocaleTimeString(undefined,{hour:"numeric",minute:"2-digit"})} – {new Date(job.end_time).toLocaleTimeString(undefined,{hour:"numeric",minute:"2-digit"})}</span></div>{job.description&&<p className="mt-3 text-sm text-slate dark:text-slate-300">{job.description}</p>}{job.cancellation_reason&&<p className="mt-3 flex items-center gap-1.5 text-xs font-semibold text-red-600"><XCircle size={14}/>{job.cancellation_reason}</p>}{job.status==="completed"&&<p className="mt-3 flex items-center gap-1.5 text-xs font-semibold text-emerald-700 dark:text-emerald-300"><CheckCircle2 size={14}/>Completed</p>}</article>)}</section></PageShell>}
