import { useCallback, useEffect, useState } from "react";
import { MapPin, Search as SearchIcon, CheckCircle2, XCircle, Bell, BellRing, Heart, SlidersHorizontal } from "lucide-react";
import { Link, useNavigate, useSearchParams } from "react-router-dom";
import PageShell from "@/components/PageShell";
import { workers as staticWorkers, type Worker } from "@/data/workers";
import type { WorkersResponse } from "@shared/api";
import { filterWorkers } from "@/lib/search";
import { supabase } from "@/lib/supabase";
import { getSavedWorkerIds, toggleSavedWorker } from "@/lib/favorites";

const categories=["All","Electrician","Painter","Plumber","Carpenter","Cleaner","Other"];
const isoToday=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`};
const addDays=(value:string,days:number)=>{const d=new Date(`${value}T00:00:00`);d.setDate(d.getDate()+days);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`};
const formatAvailabilityDate=(value?:string)=>value?new Date(`${value}T00:00:00`).toLocaleDateString(undefined,{day:"numeric",month:"short"}):"tomorrow";
const getAvailability=(worker:Worker)=>{const today=isoToday(),from=worker.away_from||"",until=worker.away_until||"",hasAway=Boolean(from&&until&&until>=from),awayToday=hasAway&&from<=today&&until>=today,startsTomorrow=hasAway&&from>today&&from<=addDays(today,1),available=Boolean(worker.available_today)&&!awayToday;if(available)return{available:true,label:"Available today",next:""};const next=worker.next_available_date||(awayToday||startsTomorrow?addDays(until,1):addDays(today,1));return{available:false,label:"Not available today",next:`Available ${formatAvailabilityDate(next)}`}};

export default function SearchResults(){
 const [searchParams]=useSearchParams();const navigate=useNavigate();const requestedService=searchParams.get("service")?.trim()||"";const requestedLocation=searchParams.get("location")?.trim()||"";
 const [availableWorkers,setAvailableWorkers]=useState<Worker[]>(staticWorkers);const [role,setRole]=useState<string|null>(null);const [watching,setWatching]=useState<Record<string,boolean>>({});const [watchingId,setWatchingId]=useState<string|null>(null);const [savedIds,setSavedIds]=useState<string[]>(getSavedWorkerIds);const [category,setCategory]=useState(requestedService||"All");const [locality,setLocality]=useState(requestedLocation);const [availability,setAvailability]=useState("all");
 const loadWorkers=useCallback(async()=>{try{const r=await fetch(`/api/workers?availability_refresh=${Date.now()}`,{cache:"no-store",headers:{"Cache-Control":"no-cache"}});if(!r.ok)throw new Error();const data=await r.json() as WorkersResponse;setAvailableWorkers(data.workers)}catch{}},[]);
 useEffect(()=>{void loadWorkers();const i=window.setInterval(()=>void loadWorkers(),10000);return()=>window.clearInterval(i)},[loadWorkers]);
 useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data})=>setRole(data.session?.user?.user_metadata?.role??null));const {data:l}=supabase.auth.onAuthStateChange((_e,s)=>setRole(s?.user?.user_metadata?.role??null));return()=>l.subscription.unsubscribe()},[]);
 useEffect(()=>{const refresh=()=>setSavedIds(getSavedWorkerIds());window.addEventListener("saved-workers-changed",refresh);return()=>window.removeEventListener("saved-workers-changed",refresh)},[]);
 const notifyMe=async(workerId:string)=>{if(!supabase)return;const {data}=await supabase.auth.getSession();if(!data.session){navigate("/login");return}if(data.session.user.user_metadata?.role!=="employer")return;setWatchingId(workerId);try{const r=await fetch("/api/notifications/watch",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${data.session.access_token}`},body:JSON.stringify({workerId})});const result=await r.json();if(!r.ok)throw new Error(result.message||"Unable to set notification");setWatching(c=>({...c,[workerId]:true}))}catch(e){window.alert(e instanceof Error?e.message:"Unable to set notification.")}finally{setWatchingId(null)}};
 const matchingWorkers=filterWorkers(availableWorkers,requestedService,requestedLocation).filter(w=>{const a=getAvailability(w);const categoryMatch=category==="All"||w.category.toLowerCase()===category.toLowerCase();const locationMatch=!locality.trim()||w.locality.toLowerCase().includes(locality.trim().toLowerCase());const availabilityMatch=availability==="all"||(availability==="available"&&a.available)||(availability==="unavailable"&&!a.available);return categoryMatch&&locationMatch&&availabilityMatch});
 const heading=requestedService?`Results for “${requestedService}”`:requestedLocation?`Workers near ${requestedLocation}`:"Workers near you";
 return <PageShell backLabel="Find Workers"><section className="rounded-[16px] border border-[#dcece7] bg-[#edf7f3] px-5 py-6 dark:border-[#3a3a3a] dark:bg-[#2f2f2f] sm:px-8 sm:py-8"><div className="flex items-start justify-between gap-4"><div><p className="text-[11px] font-bold uppercase tracking-[0.18em] text-teal dark:text-[#d1d1d1]">Search results</p><h1 className="mt-2 text-[30px] font-extrabold leading-tight tracking-[-0.045em] text-navy dark:text-[#ececec] sm:text-4xl">{heading}</h1></div><Link to="/saved" aria-label="Saved workers" className="mt-2 inline-flex shrink-0 items-center gap-2 rounded-[9px] border border-[#b9ddd4] bg-white px-3 py-2 text-[12px] font-extrabold text-teal dark:border-[#555] dark:bg-[#222] dark:text-white"><Heart size={15} fill={savedIds.length?"currentColor":"none"}/>{savedIds.length}</Link></div><div className="mt-5 grid gap-2 sm:grid-cols-[1fr_auto]"><div className="flex h-11 items-center gap-3 rounded-[10px] border border-line bg-white px-3.5 text-sm text-navy dark:border-[#424242] dark:bg-[#212121] dark:text-[#ececec]"><SearchIcon size={17} className="text-teal dark:text-[#bdbdbd]"/>{requestedService||"Any service or worker"}</div><div className="flex h-11 items-center gap-2 rounded-[10px] border border-line bg-white px-3.5 text-sm text-slate dark:border-[#424242] dark:bg-[#212121] dark:text-[#bdbdbd]"><MapPin size={17} className="text-teal dark:text-[#bdbdbd]"/>{requestedLocation||"Near you"}</div></div><div className="mt-4 rounded-[12px] border border-[#cfe4dd] bg-white/70 p-3 dark:border-white/10 dark:bg-black/20"><div className="mb-2 flex items-center gap-2 text-xs font-extrabold text-navy dark:text-white"><SlidersHorizontal size={15}/>Filter workers</div><div className="grid gap-2 sm:grid-cols-3"><select value={category} onChange={e=>setCategory(e.target.value)} aria-label="Filter by category" className="h-10 rounded-[9px] border border-line bg-white px-3 text-xs font-semibold text-navy outline-none dark:border-white/10 dark:bg-[#212121] dark:text-white">{categories.map(c=><option key={c}>{c}</option>)}</select><input value={locality} onChange={e=>setLocality(e.target.value)} placeholder="Filter by area / locality" aria-label="Filter by area" className="h-10 rounded-[9px] border border-line bg-white px-3 text-xs font-semibold text-navy outline-none dark:border-white/10 dark:bg-[#212121] dark:text-white"/><select value={availability} onChange={e=>setAvailability(e.target.value)} aria-label="Filter by availability" className="h-10 rounded-[9px] border border-line bg-white px-3 text-xs font-semibold text-navy outline-none dark:border-white/10 dark:bg-[#212121] dark:text-white"><option value="all">Any availability</option><option value="available">Available today</option><option value="unavailable">Not available today</option></select></div></div></section><section className="pt-8"><h2 className="mb-4 text-[18px] font-extrabold tracking-[-0.025em] text-navy dark:text-[#ececec]">{matchingWorkers.length} {matchingWorkers.length===1?"worker":"workers"} found</h2>{matchingWorkers.length?<div className="space-y-3">{matchingWorkers.map(worker=>{const a=getAvailability(worker);const showNotify=!a.available&&role==="employer";const saved=savedIds.includes(worker.id);return <article key={worker.id} className={`rounded-[13px] border p-4 dark:shadow-none ${a.available?"border-emerald-200 bg-emerald-50/70 dark:border-emerald-900/50 dark:bg-emerald-950/20":"border-red-200 bg-red-50/70 dark:border-red-900/50 dark:bg-red-950/20"}`}><div className="flex items-start gap-3.5"><span className="flex h-12 w-12 shrink-0 items-center justify-center overflow-hidden rounded-full bg-white text-sm font-extrabold text-navy dark:bg-[#222] dark:text-white">{worker.photo_url?<img src={worker.photo_url} alt={worker.name} className="h-full w-full object-cover"/>:worker.initials}</span><div className="min-w-0 flex-1"><h3 className="font-extrabold text-navy dark:text-[#ececec]">{worker.name}</h3><p className="mt-0.5 text-[13px] font-semibold text-teal dark:text-[#bdbdbd]">{worker.category}</p><p className="mt-2 text-[13px] text-slate dark:text-[#bdbdbd]">{worker.locality} · {worker.experience} experience</p><div className={`mt-3 flex items-center gap-1.5 text-[12px] font-extrabold ${a.available?"text-emerald-700 dark:text-emerald-300":"text-red-700 dark:text-red-300"}`}>{a.available?<CheckCircle2 size={16}/>:<XCircle size={16}/>}<span>{a.label}</span>{a.next&&<span className="font-semibold">· {a.next}</span>}</div><div className="mt-3 flex flex-wrap gap-2"><button type="button" onClick={()=>{toggleSavedWorker(worker.id);setSavedIds(getSavedWorkerIds())}} className="inline-flex items-center gap-2 rounded-[9px] border border-teal/30 bg-white/70 px-3 py-2 text-[12px] font-extrabold text-teal dark:border-white/15 dark:bg-black/20 dark:text-white"><Heart size={15} fill={saved?"currentColor":"none"}/>{saved?"Saved":"Save"}</button>{showNotify&&<button type="button" onClick={()=>notifyMe(worker.id)} disabled={watchingId===worker.id} className="inline-flex items-center gap-2 rounded-[9px] border border-teal/30 bg-white/70 px-3 py-2 text-[12px] font-extrabold text-teal dark:border-white/15 dark:bg-black/20 dark:text-white">{watching[worker.id]?<BellRing size={15}/>:<Bell size={15}/>} {watching[worker.id]?"You'll be notified when available":"Notify me when available"}</button>}</div></div><Link to={`/worker?worker=${encodeURIComponent(worker.id)}`} className="shrink-0 rounded-[9px] border border-[#b9ddd4] bg-white/70 px-3 py-2 text-[12px] font-bold text-teal dark:border-[#555] dark:bg-black/20 dark:text-[#ececec]">View Profile</Link></div></article>})}</div>:<p className="rounded-[13px] border border-line bg-white px-4 py-6 text-center text-sm text-slate dark:border-[#3a3a3a] dark:bg-[#2f2f2f] dark:text-[#bdbdbd]">No workers found for these filters.</p>}</section></PageShell>;
}
