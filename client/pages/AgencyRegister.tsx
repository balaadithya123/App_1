import { useState, type FormEvent } from "react";
import { Building2, Loader2 } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import PageShell from "@/components/PageShell";
import { supabase } from "@/lib/supabase";

const categoryOptions=["Electrician","Plumber","Carpenter","Painter","Cleaner","Other"];

export default function AgencyRegister(){
 const navigate=useNavigate(); const [busy,setBusy]=useState(false); const [error,setError]=useState(""); const [success,setSuccess]=useState(""); const [categories,setCategories]=useState<string[]>([]);
 const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setError("");setSuccess("");if(!supabase){setError("Registration is not configured yet.");return;}const form=new FormData(event.currentTarget);const name=String(form.get("name")||"").trim();const contactPersonName=String(form.get("contactPersonName")||"").trim();const phone=String(form.get("phone")||"").replace(/\D/g,"");const email=String(form.get("email")||"").trim();const serviceLocations=String(form.get("serviceLocations")||"").split(",").map(v=>v.trim()).filter(Boolean);const teamSizeBand=String(form.get("teamSizeBand")||"");const businessRegistrationNumber=String(form.get("businessRegistrationNumber")||"").trim();const logoUrl=String(form.get("logoUrl")||"").trim();const description=String(form.get("description")||"").trim();if(!/^\d{10}$/.test(phone)){setError("Phone number must be exactly 10 digits.");return}if(!contactPersonName){setError("Contact person name is required.");return}if(!categories.length){setError("Select at least one service category.");return}if(!serviceLocations.length){setError("Add at least one service area.");return}setBusy(true);try{const {data,error:authError}=await supabase.auth.signUp({email,password:phone+"LW!"+email.slice(0,2),options:{data:{role:"agency",name,phone,location:serviceLocations[0]}}});if(authError)throw authError;if(!data.session){setSuccess("Agency account created. Check your email if confirmation is required, then sign in to finish your profile.");return}const response=await fetch("/api/agencies/register",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${data.session.access_token}`},body:JSON.stringify({name,contactPersonName,phone,email,categories,serviceLocations,teamSizeBand,businessRegistrationNumber,logoUrl,description})});const result=await response.json().catch(()=>null);if(!response.ok)throw new Error(result?.message||"Unable to save agency profile.");navigate("/agency",{replace:true})}catch(submitError){setError(submitError instanceof Error?submitError.message:"Registration failed. Please try again.")}finally{setBusy(false)}};
 return <PageShell backTo="/register" backLabel="Choose registration type"><section className="rounded-[16px] border border-[#dcece7] bg-[#edf7f3] px-5 py-7 dark:border-white/10 dark:bg-black sm:px-8 sm:py-9"><div className="flex items-center gap-3"><Building2 size={32} className="text-navy dark:text-white"/><div><h1 className="text-[30px] font-extrabold leading-tight tracking-[-0.045em] text-navy dark:text-white">Register as an Agency</h1><p className="mt-2 text-sm text-slate dark:text-slate-300">Create an agency profile for your team and services.</p></div></div></section><form onSubmit={submit} className="mt-7 space-y-4 rounded-[13px] border border-line bg-white p-5 shadow-sm dark:border-white/10 dark:bg-black sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><Field name="name" label="Agency Name" required placeholder="Your agency name"/><Field name="contactPersonName" label="Contact Person Name" required placeholder="Person responsible for the agency"/><Field name="phone" label="Phone Number" required placeholder="10-digit mobile number" numeric/><Field name="email" label="Email Address" required type="email" placeholder="agency@example.com"/></div><div><label className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">Service Categories</label><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{categoryOptions.map(c=><label key={c} className="flex cursor-pointer items-center gap-2 rounded-[9px] border border-line px-3 py-2.5 text-sm dark:border-white/10"><input type="checkbox" checked={categories.includes(c)} onChange={()=>setCategories(v=>v.includes(c)?v.filter(x=>x!==c):[...v,c])}/><span className="dark:text-white">{c}</span></label>)}</div></div><div><label className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">Service Areas</label><input name="serviceLocations" required placeholder="e.g. Kattur, Trichy, Srirangam" className="h-11 w-full rounded-[9px] border border-line bg-[#fbfcfc] px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/><p className="mt-1 text-xs text-slate">Separate locations with commas.</p></div><div><label className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">Team Size</label><select name="teamSizeBand" defaultValue="2-5" className="h-11 w-full rounded-[9px] border border-line bg-[#fbfcfc] px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"><option value="2-5">2–5 workers</option><option value="6-15">6–15 workers</option><option value="15+">15+ workers</option></select></div><div className="grid gap-4 sm:grid-cols-2"><Field name="businessRegistrationNumber" label="Business Registration / GST (optional)" placeholder="Optional"/><Field name="logoUrl" label="Logo URL (optional)" placeholder="Optional image URL"/></div><div><label className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">About the Agency</label><textarea name="description" rows={4} placeholder="Tell people what your agency does" className="w-full resize-none rounded-[9px] border border-line bg-[#fbfcfc] px-3 py-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/></div>{error&&<p role="alert" className="text-center text-[13px] font-semibold text-red-600">{error}</p>}{success&&<p role="status" className="rounded-[9px] bg-emerald-50 px-3 py-2.5 text-center text-[13px] font-semibold text-emerald-700">{success}</p>}<button disabled={busy} className="flex h-12 w-full items-center justify-center gap-2 rounded-[10px] bg-navy text-sm font-bold text-white disabled:opacity-70">{busy?<><Loader2 size={17} className="animate-spin"/> Creating agency...</>:"Register Agency"}</button><p className="text-center text-sm text-slate">Already registered? <Link to="/login" className="font-bold text-navy dark:text-white">Sign in</Link></p></form></PageShell>;
}
function Field({name,label,required,placeholder,type="text",numeric=false}:{name:string;label:string;required?:boolean;placeholder:string;type?:string;numeric?:boolean}){return <div><label className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">{label}</label><input name={name} required={required} type={type} inputMode={numeric?"numeric":undefined} maxLength={numeric?10:undefined} onInput={numeric?e=>{e.currentTarget.value=e.currentTarget.value.replace(/\D/g,"").slice(0,10)}:undefined} placeholder={placeholder} className="h-11 w-full rounded-[9px] border border-line bg-[#fbfcfc] px-3 text-sm outline-none dark:border-white/10 dark:bg-[#050505] dark:text-white"/></div>}
