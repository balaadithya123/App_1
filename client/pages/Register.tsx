import { useState } from "react";
import { useNavigate } from "react-router-dom";
import PageShell from "@/components/PageShell";

export default function Register() {
  const navigate = useNavigate();
  const [role, setRole] = useState<"worker" | "employer" | null>(null);

  if (!role) return <PageShell><section className="rounded-[16px] border border-line bg-background px-5 py-8 text-center dark:border-slate-800 dark:bg-[#0a0c10] sm:px-8 sm:py-10"><h1 className="text-3xl font-extrabold text-navy dark:text-white sm:text-4xl">Register</h1><p className="mx-auto mt-3 max-w-xl text-sm text-slate dark:text-slate-300">Choose how you want to use Local Worker Discovery.</p><div className="mx-auto mt-8 grid max-w-3xl gap-5 sm:grid-cols-2"><button type="button" onClick={()=>setRole("worker")} className="rounded-2xl border border-line bg-card p-7 text-left shadow-sm hover:shadow-md dark:border-slate-700 dark:bg-[#11141a]"><div className="mb-4 text-4xl">👷</div><h2 className="text-xl font-extrabold text-navy dark:text-white">Register as Worker</h2><p className="mt-2 text-sm text-slate dark:text-slate-300">Create a profile so nearby people can find your skills and services.</p><span className="mt-5 inline-block font-bold text-navy dark:text-white">Continue →</span></button><button type="button" onClick={()=>setRole("employer")} className="rounded-2xl border border-line bg-card p-7 text-left shadow-sm hover:shadow-md dark:border-slate-700 dark:bg-[#11141a]"><div className="mb-4 text-4xl">🏢</div><h2 className="text-xl font-extrabold text-navy dark:text-white">Register as Employer</h2><p className="mt-2 text-sm text-slate dark:text-slate-300">Create an employer account to find and contact local workers.</p><span className="mt-5 inline-block font-bold text-navy dark:text-white">Continue →</span></button></div></section></PageShell>;

  return <PageShell><section className="rounded-[16px] border border-line bg-background px-5 py-7 dark:border-slate-800 dark:bg-[#0a0c10]"><button type="button" onClick={()=>setRole(null)} className="mb-5 text-sm font-bold text-navy dark:text-white">← Back</button><h1 className="text-3xl font-extrabold text-navy dark:text-white">Register as {role === "worker" ? "a Worker" : "an Employer"}</h1><p className="mt-3 text-sm text-slate dark:text-slate-300">Enter your email and request an OTP. The OTP field appears only after you press the button.</p></section><form onSubmit={e=>{e.preventDefault();navigate(`/register/${role}`)}} className="mt-7 space-y-5 rounded-[13px] border border-line bg-card p-5 dark:border-slate-800 dark:bg-[#0a0c10] sm:p-7"><div><label htmlFor="email" className="mb-2 block text-sm font-bold text-navy dark:text-white">Email Address</label><input id="email" type="email" required placeholder="you@example.com" className="h-11 w-full rounded-[9px] border border-line bg-background px-3 text-sm text-navy outline-none dark:border-slate-700 dark:bg-[#11141a] dark:text-white"/></div><button type="submit" className="h-12 w-full rounded-[10px] bg-navy text-sm font-bold text-white">Continue &amp; Send OTP</button></form></PageShell>;
}
