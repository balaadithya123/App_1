import { useState, type FormEvent } from "react";
import PageShell from "@/components/PageShell";
import type { ApiErrorResponse, WorkerRegistrationRequest } from "@shared/api";
const fields = [["fullName","Full Name","e.g. Ravi Kumar"],["phone","Phone Number","10-digit mobile number"],["category","Work Category","e.g. Electrician"],["location","Location","Your town or locality"],["experience","Years of Experience","e.g. 5 years"],["services","Services Offered","e.g. Wiring, repairs"]] as const;
export default function Register(){const[submitted,setSubmitted]=useState(false),[isSubmitting,setIsSubmitting]=useState(false),[error,setError]=useState("");const handleSubmit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setError("");const formData=new FormData(event.currentTarget),phone=String(formData.get("phone")||"").replace(/\D/g,"");if(!/^\d{10}$/.test(phone)){setError("Phone number must be exactly 10 digits.");return}setIsSubmitting(true);const payload:WorkerRegistrationRequest={fullName:String(formData.get("fullName")||""),phone,category:String(formData.get("category")||""),location:String(formData.get("location")||""),experience:String(formData.get("experience")||""),services:String(formData.get("services")||""),about:String(formData.get("about")||"")};try{const response=await fetch("/api/workers/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)}),data=await response.json().catch(()=>null) as ApiErrorResponse|null;if(!response.ok)throw new Error(data?.message||`Registration failed (HTTP ${response.status}).`);setSubmitted(true)}catch(error){setError(error instanceof Error?error.message:"Registration failed. Please try again.")}finally{setIsSubmitting(false)}};return <PageShell><section className="rounded-[16px] border border-[#dcece7] bg-[#edf7f3] px-5 py-7 dark:border-blue-900/50 dark:bg-[#0a0c10] sm:px-8 sm:py-9"><h1 className="text-[30px] font-extrabold leading-tight tracking-[-0.045em] text-navy dark:text-white sm:text-4xl">Register as a Worker</h1><p className="mt-3 text-[14px] leading-6 text-slate dark:text-slate-300">Tell people nearby what kind of work you can help with.</p></section>{submitted?<section className="mt-6 rounded-[13px] border border-[#b9ddd4] bg-mint p-5 text-center dark:border-blue-900/50 dark:bg-[#07121d]"><h2 className="font-extrabold text-navy dark:text-white">Your registration has been submitted.</h2></section>:<form onSubmit={handleSubmit} className="mt-7 space-y-4 rounded-[13px] border border-line bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-[#0a0c10] sm:p-7">{fields.map(([id,label,placeholder])=><div key={id}><label htmlFor={id} className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">{label}</label><input id={id} name={id} required inputMode={id==="phone"?"numeric":undefined} maxLength={id==="phone"?10:undefined} pattern={id==="phone"?"[0-9]{10}":undefined} onInput={id==="phone"?(e)=>{e.currentTarget.value=e.currentTarget.value.replace(/\D/g,"").slice(0,10)}:undefined} placeholder={placeholder} className="h-11 w-full rounded-[9px] border border-line bg-[#fbfcfc] px-3 text-sm text-navy outline-none dark:border-slate-700 dark:bg-[#11141a] dark:text-white"/></div>)}<div><label htmlFor="about" className="mb-2 block text-[13px] font-bold text-navy dark:text-slate-100">About You</label><textarea id="about" name="about" required placeholder="A short introduction" rows={4} className="w-full resize-none rounded-[9px] border border-line bg-[#fbfcfc] px-3 py-3 text-sm text-navy outline-none dark:border-slate-700 dark:bg-[#11141a] dark:text-white"/></div>{error&&<p className="text-center text-[13px] font-semibold text-red-600 dark:text-red-400">{error}</p>}<button type="submit" disabled={isSubmitting} className="flex h-12 w-full items-center justify-center rounded-[10px] bg-navy text-sm font-bold text-white disabled:opacity-80">{isSubmitting?"Submitting...":"Register as a Worker"}</button></form>}</PageShell>}
