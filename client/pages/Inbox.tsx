import { useEffect, useState } from "react";
import { Bell, CheckCircle2, Mail, RefreshCw } from "lucide-react";
import { useNavigate } from "react-router-dom";
import PageShell from "@/components/PageShell";
import { supabase } from "@/lib/supabase";

type Notification={id:string;type:string;title:string;message:string;worker_id:string|null;read_at:string|null;created_at:string};
export default function Inbox(){const navigate=useNavigate();const [items,setItems]=useState<Notification[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const load=async()=>{if(!supabase)return;const {data}=await supabase.auth.getSession();if(!data.session){navigate("/",{replace:true});return;}try{const response=await fetch("/api/notifications",{headers:{Authorization:`Bearer ${data.session.access_token}`},cache:"no-store"});const result=await response.json();if(!response.ok)throw new Error(result.message||"Unable to load inbox");setItems(result.notifications||[]);setError("");}catch(e){setError(e instanceof Error?e.message:"Unable to load inbox.");}finally{setLoading(false);}};useEffect(()=>{void load();const listener=supabase?.auth.onAuthStateChange((event,session)=>{if(event==="SIGNED_OUT"||!session){navigate("/",{replace:true});}});return()=>listener?.data.subscription.unsubscribe();},[navigate]);const markRead=async(id:string)=>{const {data}=await supabase!.auth.getSession();if(!data.session)return;await fetch("/api/notifications/read",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${data.session.access_token}`},body:JSON.stringify({id})});setItems(current=>current.map(item=>item.id===id?{...item,read_at:new Date().toISOString()}:item));};return <PageShell backLabel="Find Workers"><section className="rounded-[18px] border border-line bg-white p-5 dark:border-white/10 dark:bg-[#111] sm:p-7"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-navy text-white"><Bell size={20}/></div><div className="min-w-0 flex-1"><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-teal">Employer Inbox</p><h1 className="mt-1 text-3xl font-extrabold text-navy dark:text-white">Notifications</h1><p className="mt-1 text-sm text-slate dark:text-slate-300">Updates about workers you asked us to watch.</p></div><button onClick={()=>void load()} className="rounded-[9px] border border-line p-2.5 text-slate dark:border-white/10 dark:text-slate-300" aria-label="Refresh inbox"><RefreshCw size={17}/></button></div></section><section className="mt-5 space-y-3">{loading?<p className="rounded-[14px] border border-line bg-white p-7 text-center text-sm text-slate dark:border-white/10 dark:bg-[#111] dark:text-slate-300">Loading inbox...</p>:error?<p className="rounded-[14px] border border-red-200 bg-red-50 p-5 text-sm text-red-700 dark:border-red-900/40 dark:bg-red-950/20 dark:text-red-300">{error}</p>:items.length===0?<div className="rounded-[14px] border border-line bg-white p-8 text-center dark:border-white/10 dark:bg-[#111]"><Mail className="mx-auto text-slate" size={27}/><p className="mt-3 font-bold text-navy dark:text-white">Your inbox is empty</p><p className="mt-1 text-sm text-slate dark:text-slate-400">Tap “Notify me when available” on an unavailable worker to receive an update here.</p></div>:items.map(item=><article key={item.id} className={`rounded-[14px] border p-4 ${item.read_at?"border-line bg-white dark:border-white/10 dark:bg-[#111]":"border-teal/30 bg-teal/5 dark:border-teal/30 dark:bg-teal/10"}`}><div className="flex gap-3"><span className="mt-0.5 flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-white text-teal shadow-sm dark:bg-black/30"><Bell size={16}/></span><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div><h2 className="text-sm font-extrabold text-navy dark:text-white">{item.title}</h2><p className="mt-1 text-sm text-slate dark:text-slate-300">{item.message}</p></div>{!item.read_at&&<span className="mt-1 h-2.5 w-2.5 shrink-0 rounded-full bg-teal"/>}</div><div className="mt-3 flex items-center justify-between gap-2"><p className="text-[11px] text-slate dark:text-slate-500">{new Date(item.created_at).toLocaleString()}</p>{!item.read_at&&<button onClick={()=>void markRead(item.id)} className="inline-flex items-center gap-1.5 text-[11px] font-extrabold text-teal"><CheckCircle2 size={14}/>Mark read</button>}</div></div></div></article>)}</section></PageShell>}
