{
  "installCommand": "npm install",
  "buildCommand": "npm run build:client && npm run build:vercel-server",
  "outputDirectory": "dist/spa",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "headers": [
    { "source": "/api/(.*)", "headers": [{ "key": "Cache-Control", "value": "no-store" }] }
  ]
}
